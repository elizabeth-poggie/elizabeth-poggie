/**
 * Create a user object that holds your player's stats
 * then save this object to `localStorage`.
 *
 * After that, retrieve the stats from `localStorage` and log them to the console.
 *
 * Food for thought: How would you check what's currently stored in `localStorage`?
 */

localStorage.setItem("what-is-this?", JSON.stringify(user));
const user = localStorage.getItem("what-is-this?");

/**
 *
 * TODO - insert code below
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 */
